# HOST SCHEDULER FOR SQUID

This repository is aim to provide the script to generate IP and hostname based on the available partner hostname on /etc/squid/squid.conf and replace the A record to /etc/hosts.  
